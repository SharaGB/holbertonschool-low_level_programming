#include "main.h"
/**
 * main - Prints its name
 * @argc: Check argc
 * @argv: Check of string
 * Return: 0
 */
int main(int argc, char **argv)
{
	int i = 0;

	if (argc == 1)
	{
		printf("%s\n", argv[i]);
	}
	return (0);
}
